/*
 * clock_config.c
 *
 * Clock configuration functions for MSPM0G350X
 */

#include "ti_msp_dl_config.h"

void Clock_Set_80MHz(void)
{
    // 简化的时钟设置实现
    // 注意：实际的80MHz设置需要通过SysConfig工具配置
    // 这里只是一个占位符函数
    
    // 可以在这里添加一些基本的时钟配置
    // 但复杂的PLL配置应该通过SysConfig完成
}